# Experiment Registry

`registry.jsonl` is append-only. Each actual experiment receives an ID such as
`EXP-2026-001`, a preregistration record conforming to
`schemas/experiment.schema.json`, and an immutable evidence directory.

Never replace, delete, or rewrite an unfavorable experiment. Append a new
record that supersedes it and explains why.

